version: 2

jobs:
  linux:
    docker:
      - image: swift:5.0
    steps:
      - checkout
      - run: swift build -v
      - run: swift test -v

workflows:
  version: 2
  tests:
    jobs:
      - linux
